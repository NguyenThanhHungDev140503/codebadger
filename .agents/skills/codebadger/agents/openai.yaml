interface:
  display_name: "Codebadger"
  short_description: "Repo-specific patterns and workflows for codebadger"
  default_prompt: "Use the codebadger repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true